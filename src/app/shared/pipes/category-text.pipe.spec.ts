import { CategoryTextPipe } from './category-text.pipe';

describe('CategoryTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTextPipe();
    expect(pipe).toBeTruthy();
  });
});
